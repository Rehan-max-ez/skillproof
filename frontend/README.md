
  # SkillProof UI Design

  This is a code bundle for SkillProof UI Design. The original project is available at https://www.figma.com/design/7yOTOr3WvV94SNtkxBp5ad/SkillProof-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  